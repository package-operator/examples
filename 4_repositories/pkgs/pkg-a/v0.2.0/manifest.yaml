apiVersion: manifests.package-operator.run/v1alpha1
kind: PackageManifest
metadata:
  name: pkg-a
  annotations:
    version: v0.2.0
spec:
  scopes:
  - Cluster
  - Namespaced
  phases:
  - name: deploy
  constraints:
  - platform:
    - OpenShift
  - platformVersion:
      name: OpenShift
      range: 4.12.x - 4.14.x && != 4.13.5